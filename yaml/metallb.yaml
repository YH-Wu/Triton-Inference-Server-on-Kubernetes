configInline:
  address-pools:
  - name: default
    protocol: layer2

    #IP segment should be same as management segment
    addresses:
    - <IP_RANGE>-<IP_RANGE> 

controller:
  nodeSelector:
    node-role.kubernetes.io/master: ""

